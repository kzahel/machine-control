{
  "name": "ios-device-testbed",
  "version": "0.1.0",
  "private": true,
  "description": "Project-neutral physical iPhone automation testbed",
  "license": "MIT",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "python3 -m unittest discover -s tests -v",
    "check": "python3 -m compileall -q ios_device.py tests && python3 -m unittest discover -s tests -v"
  },
  "dependencies": {
    "agent-device": "0.20.5"
  }
}
